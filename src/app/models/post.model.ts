export interface Post {
  postId: string;
  postTitle: string;
  postContent: string;
  postImageUrl: string;
  postedById: string;
  postedBy: string;
  postedByPhoto: string;
  postOrgId: string;
  createdAt: number;
}
